# Развертывание (Deployment)

"Novella" — это автономное консольное приложение. Его не нужно "развертывать" на сервере; его нужно "опубликовать" как исполняемый файл для конечных пользователей (игроков).

Мы будем использовать команду `dotnet publish` для создания готового к запуску файла.

## 🎯 Целевые платформы

Благодаря .NET, мы можем легко скомпилировать игру для Windows, macOS и Linux.

## 📦 Процесс публикации

Команда `dotnet publish` соберет проект и включит в папку `publish` все необходимые файлы, включая среду выполнения .NET. Это называется **"self-contained" (автономное) приложение**.

### 1. Публикация для Windows (x64)

```bash
# -c Release = Собрать в режиме релиза (оптимизированный)
# -r win-x64 = Указать Runtime Identifier (RID) для Windows 64-bit
dotnet publish -c Release -r win-x64
- **Результат:** Исполняемый файл `Novella.exe` (и другие файлы) появится в папке `/bin/Release/net6.0/win-x64/publish/`.
    
```
### 2. Публикация для Linux (x64)

```Bash
dotnet publish -c Release -r linux-x64
```
```bash

- **Результат:** Исполняемый файл `Novella` (без расширения) появится в `/bin/Release/net6.0/linux-x64/publish/`.
    
```
### 3. Публикация для macOS (x64)

```Bash
dotnet publish -c Release -r osx-x64
```
- **Результат:** Исполняемый файл `Novella` появится в `/bin/Release/net6.0/osx-x64/publish/`.
    

## 🎁 Распространение (Distribution)

1. После запуска команды `publish` для нужной платформы, перейдите в соответствующую папку `publish/`.
    
2. Заархивируйте **все содержимое** этой папки в один `.zip` файл (например, `Novella-Windows-x64.zip`).
    
3. Этот ZIP-архив готов для загрузки на такие платформы, как [Itch.io](https://itch.io/), [Game Jolt](https://gamejolt.com/) или в раздел "Releases" на GitHub. 
