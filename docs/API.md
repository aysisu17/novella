# API и Узлы Сюжета

Поскольку "Novella" является консольным приложением, а не библиотекой, у него нет публичного API в традиционном смысле. Этот документ описывает внутреннюю структуру "узлов" (шагов) сюжета и концовок, как они определены в `Program.cs`.

## 📍 Узлы Сюжета (Метки `Step`)

Каждая метка `Step` представляет собой уникальный момент в игре, где игрок должен сделать выбор.

| Метка | Описание | Выборы (Ведут к...) |
| :--- | :--- | :--- |
| `Step1:` | **Вход в пещеру.** | 1: `Step2` (Зайти) <br> 2: `Ending_Coward` (Уйти) |
| `Step2:` | **Главный зал.** | 1: `Step3` (Кристалл) <br> 2: `Step4` (Левый туннель) <br> 3: `Step5` (Правый туннель) |
| `Step3:` | **Кристалл.** | *Нет выбора.* Получает `hasOrb = true`. <br> -> `Step2` |
| `Step4:` | **Левый туннель.** | *Нет выбора.* <br> -> `Step6` |
| `Step5:` | **Правый туннель.** | *Нет выбора.* (Обвал) <br> -> `Step6` |
| `Step6:` | **Подземное озеро.** | 1: `Step7` (Искать) <br> 2: `Step8` (Переплыть) |
| `Step7:` | **Поиск в воде.** | *Нет выбора.* (Находит меч - *примечание: меч пока не используется*) <br> -> `Step8` |
| `Step8:` | **Другая сторона озера.** | 1: `Step9` (Деревянная дверь) <br> 2: `Step10` (Металлическая дверь) |
| `Step9:` | **Деревянная дверь.** | *Нет выбора.* (Ловушка) <br> -> `Ending_Void` |
| `Step10:` | **Комната Стража.** | 1: `Step11` (Прокрасться) <br> 2: `Step12` (Разбудить) |
| `Step11:` | **Прокрасться мимо.** | *Нет выбора.* <br> -> `Step13` |
| `Step12:` | **Разбудить стража.** | *Нет выбора.* (Поражение) <br> -> `Ending_Captured` |
| `Step13:` | **Финальный зал.** | *Нет выбора.* <br> -> `Step14` |
| `Step14:` | **Барьер.** | *Проверка условия.* <br> (Если `hasOrb == true`) -> `Step15` <br> (Если `hasOrb == false`) -> `Ending_Trapped` |
| `Step15:` | **Артефакт.** | *Нет выбора.* (Победа) <br> -> `Ending_Victory` |

## 🏁 Концовки (Метки `Ending`)

| Метка             | Название  | Описание                                             |
| :---------------- | :-------- | :--------------------------------------------------- |
| `Ending_Coward`   | Трус      | Игрок отказывается войти в пещеру.                   |
| `Ending_Void`     | Падение   | Игрок открывает дверь-ловушку и падает в бездну.     |
| `Ending_Captured` | Пленник   | Игрок будит стража и попадает в плен.                |
| `Ending_Trapped`  | В западне | Игрок доходит до барьера без Светящейся Сферы.       |
| `Ending_Victory`  | Победа    | Игрок использует Сферу, забирает артефакт и сбегает. |