# Тестирование

На данный момент проект "Novella" тестируется **вручную**. Из-за использования `Console.ReadLine()` и `goto` автоматическое модульное тестирование (Unit Testing) затруднено.

## 🔬 План ручного тестирования (QA)

Цель состоит в том, чтобы проверить **каждую** сюжетную ветку и **каждую** концовку.

### Тестовые сценарии (Пути к концовкам)
Пройдите игру, следуя этим "сценариям", чтобы убедиться, что каждая концовка достигается правильно.

|**ID**|**Цель**|**Шаги (Ввод)**|**Ожидаемый результат**|
|---|---|---|---|
|T1|**Концовка: Трус**|На `Step1` -> Ввести `2`|`Ending_Coward` (Текст: "Вы вернулись домой...")|
|T2|**Концовка: Падение**|`1` (Вход) -> `2` (Левый) -> `1` (Искать) -> `1` (Деревянная дверь)|`Ending_Void` (Текст: "Вы упали в бездну...")|
|T3|**Концовка: Пленник**|`1` (Вход) -> `3` (Правый) -> `2` (Переплыть) -> `2` (Металлическая дверь) -> `2` (Разбудить стража)|`Ending_Captured` (Текст: "Страж пленил вас...")|
|T4|**Концовка: В западне (Нет Сферы)**|`1` (Вход) -> `2` (Левый) -> `2` (Переплыть) -> `2` (Металлическая дверь) -> `1` (Прокрасться)|`Ending_Trapped` (Текст: "Вы заперты в последнем зале...")|
|T5|**Концовка: Победа (Есть Сфера)**|`1` (Вход) -> **`1` (Берем Сферу)** -> `2` (Левый) -> `2` (Переплыть) -> `2` (Металлическая дверь) -> `1` (Прокрасться)|`Ending_Victory` (Текст: "Вы выбрались из пещеры...")|
### Тестирование некорректного ввода

На каждом шаге (`Step`), где требуется ввод, введите что-то, чего нет в списке (например, `5`, `abc`, ).

- **Ожидаемый результат:** Игра должна вывести "Неверный ввод" и **повторить** тот же самый шаг (вернуться к той же метке `Step`).

---
## 🔮 Будущее тестирования (Автоматизация)

Чтобы сделать проект тестируемым, потребуется **рефакторинг** (см. `ARCHITECTURE.md`):

1. **Выделить логику:** Изолировать логику игры (обработку выбора, изменение состояния) от прямого ввода/вывода через `Console.Read/Write`.
    
2. **Создать `GameEngine`:** Создать класс `GameEngine` с методом, например `ProcessInput(string input)`.
    
3. **Unit-тесты:** Мы сможем написать тесты, которые вызывают `ProcessInput` и проверяют, в каком состоянии (`Scene`) оказалась игра, не взаимодействуя с консолью.